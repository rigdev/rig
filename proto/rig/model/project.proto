syntax = "proto3";

package model;

message ProjectFilter {
  message All {}

  message Selected {
    repeated string project_ids = 1;
  }

  oneof filter {
    All all = 1;
    Selected selected = 2;
  }
}